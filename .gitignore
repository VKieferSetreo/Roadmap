# Dependencies
node_modules/
bun.lockb

# Lokale Arbeits-Artefakte (kein Produktcode)
.planning/
.claude-kanban/

# Interne Konzept-/Orientierungsdokumente (nicht committen)
_konzept/

# Build output
dist/
dist-ssr/
*.local
*.tsbuildinfo

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment (real secrets — only .env.example commited)
.env
.env.local
.env.*.local
# T-278: auch benannte .env in Unterordnern (z.B. API/.../mobilithek.env) — die enthielten echte
# Secrets und waren getrackt. *.env matcht NICHT *.env.example (anderes Suffix).
*.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
dist-share/
certificate.p12
*.p12
*.pfx
mob_*.pem

# BASt-LCL Rohdaten (nur generierte server/src/data/tmc_de.json wird committed)
LCL22_deutsch/

# Playwright MCP snapshot artifacts
.playwright-mcp/
*.gpkg

# VEMAGS-Bescheide = sensible Kundendaten, NIE committen (T-567)
VEMAGS.pdf
*.vemags.pdf
