FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

ENV NODE_ENV=production

# KEINE Migration on-boot — die macht die API-App (Dockerfile).
# Der Worker wartet selbst via Schema-Retry, bis migrations/003 (quellen) da ist.
CMD ["node", "src/worker/index.js"]
